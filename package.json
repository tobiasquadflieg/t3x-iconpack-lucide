{
  "name": "iconpack-lucide",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build:lucide",
    "build:lucide": "bun run Build/copy-lucide.ts",
    "postversion": "bun run Build/update-version.ts",
    "update": "bun install && bun run build && bun run postversion"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "lucide-static": "^0.563.0"
  }
}
